This program allows you to generate a random password using numbers, letters, and special characters.
All you need to do is specify the desired password length.
The maximum length is 100 characters, but you can easily change it by modifying the `MAX_LENGTH` macro in the source code.
